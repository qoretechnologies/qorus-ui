stages:
  - build

build_package:
  stage: build
  image: alpine:latest
  tags:
    - docker-exec
  before_script:
    - apk add yarn
  script:
    - yarn install
    - yarn build
  artifacts:
    paths:
      - dist/
  only:
    - '4.0'
